{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "description": "MiniRedis Visualizer API — an in-memory key-value store with TTL, exposed over a small REST API.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
